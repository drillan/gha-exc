name: merge draft
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: |
          declare -A exam
          exam["suzuki"]=80
          exam["tanaka"]=95
          exam["yamamoto"]=90
          for e in ${exam[@]} 
          do 
            echo $e
          done
        env:
          NAMES: AAA BBB
